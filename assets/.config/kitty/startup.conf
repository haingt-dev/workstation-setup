os_window_state maximized

layout splits

# 1. DIC (left) — claude session in digital-identity
launch --title "digital-identity" --cwd ~/Projects/digital-identity zsh -c 'source ~/.zshrc; claude --permission-mode auto --continue; exec zsh'

# 2. Shell (right, full height) — main
launch --title "shell" --location=vsplit --cwd ~/Projects
