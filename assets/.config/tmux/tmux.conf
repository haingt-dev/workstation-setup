# =============================================================================
# Enhanced tmux Configuration with TPM and Catppuccin
# =============================================================================

# -----------------------------------------------------------------------------
# General Settings
# -----------------------------------------------------------------------------

# Enable true color support
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*256col*:Tc"
set -ga terminal-overrides ",xterm-kitty:Tc"

# Enable mouse support
set -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Renumber windows when one is closed
set -g renumber-windows on

# Increase history limit
set -g history-limit 50000

# Faster key repetition
set -s escape-time 0

# Enable focus events
set -g focus-events on

# Allow kitty graphics protocol passthrough (for fastfetch logo, etc.)
set -g allow-passthrough on

# Forward extended keys (Ctrl+Enter, etc.) to applications
set -g extended-keys always
set -s extended-keys-format csi-u
set -ga update-environment "TERM_PROGRAM TERM_PROGRAM_VERSION"

# Aggressive resize
setw -g aggressive-resize on

# -----------------------------------------------------------------------------
# Key Bindings
# -----------------------------------------------------------------------------

# Change prefix to Ctrl-a (more ergonomic than Ctrl-b)
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Reload config
bind r source-file ~/.config/tmux/tmux.conf \; display "Config reloaded!"

# Split panes using | and -
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# Navigate panes with vim keys
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize panes with vim keys
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# New window in current path
bind c new-window -c "#{pane_current_path}"

# Quick window selection
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# Swap windows
bind -r "<" swap-window -d -t -1
bind -r ">" swap-window -d -t +1

# Toggle last window
bind Space last-window

# Synchronize panes
bind S setw synchronize-panes

# Kill pane/window without confirmation
bind x kill-pane
bind X kill-window

# -----------------------------------------------------------------------------
# Copy Mode (Vi style)
# -----------------------------------------------------------------------------

setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-pipe-and-cancel 'xclip -in -selection clipboard'
bind -T copy-mode-vi C-v send -X rectangle-toggle
bind -T copy-mode-vi Escape send -X cancel

# Enter copy mode with Ctrl-a [
bind [ copy-mode
bind ] paste-buffer

# -----------------------------------------------------------------------------
# Session Management
# -----------------------------------------------------------------------------

# Create new session
bind N new-session

# Switch sessions with fzf popup
bind s display-popup -E -w 80% -h 60% "\
  tmux list-sessions -F '#{session_name}: #{session_windows} windows' \
  | fzf --reverse --header='Switch Session' \
  | cut -d: -f1 \
  | xargs tmux switch-client -t"

# Sync lazygit pane to current directory (quit + wrapper restarts)
bind g run-shell 'tmux send-keys -t main:1.2 q'

# Yazi file manager popup (opens in current pane's directory)
bind y display-popup -E -w 80% -h 80% -d '#{pane_current_path}' '$HOME/.local/bin/yazi'

# Switch windows with fzf popup
bind w display-popup -E -w 80% -h 60% "\
  tmux list-windows -a -F '#{session_name}:#{window_index} #{window_name}' \
  | fzf --reverse --header='Switch Window' \
  | cut -d' ' -f1 \
  | xargs tmux switch-client -t"

# -----------------------------------------------------------------------------
# Catppuccin Theme Configuration
# -----------------------------------------------------------------------------

set -g @catppuccin_flavor 'mocha'
set -g @catppuccin_window_status_style "rounded"

# Window styling
set -g @catppuccin_window_number_position "left"
set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W"
set -g @catppuccin_window_flags "icon"

# Status bar modules
set -g @catppuccin_status_modules_left "application"
set -g @catppuccin_status_modules_right "host directory session date_time"
set -g @catppuccin_status_left_separator  ""
set -g @catppuccin_status_right_separator ""
set -g @catppuccin_status_fill "all"
set -g @catppuccin_status_connect_separator "yes"

# Date/time format
set -g @catppuccin_date_time_text "%d/%m %H:%M"

# Directory — basename only to avoid overflow
set -g @catppuccin_directory_text "#{b:pane_current_path}"

# -----------------------------------------------------------------------------
# Resurrect & Continuum Settings
# -----------------------------------------------------------------------------

# Resurrect settings
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-strategy-nvim 'session'
set -g @resurrect-strategy-vim 'session'

# Continuum settings
# Disabled: .zshrc handles layout creation, continuum-restore conflicts with it
# Manual restore still works via prefix + Ctrl-r
set -g @continuum-restore 'off'
set -g @continuum-save-interval '15'

# -----------------------------------------------------------------------------
# Status Bar Position
# -----------------------------------------------------------------------------
set -g status-position bottom
set -g status-interval 5

# -----------------------------------------------------------------------------
# Pane Borders (Catppuccin colors)
# -----------------------------------------------------------------------------
set -g pane-border-style 'fg=#45475a'
set -g pane-active-border-style 'fg=#89b4fa'

# -----------------------------------------------------------------------------
# Message Style (Catppuccin colors)
# -----------------------------------------------------------------------------
set -g message-style 'bg=#313244 fg=#cdd6f4'

# -----------------------------------------------------------------------------
# Tmux Plugin Manager (TPM)
# -----------------------------------------------------------------------------

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'catppuccin/tmux#v2.1.0'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'