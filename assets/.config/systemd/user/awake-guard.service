[Unit]
Description=Block auto-suspend while a Claude Code session is in use

[Service]
Type=exec
ExecStart=%h/.local/bin/awake-guard.sh
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
